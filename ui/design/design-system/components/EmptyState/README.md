# EmptyState

An empty panel.

- **Props:** `title`, `children`, `action`, `glyph`.
- Say what is empty, why, and the next step ("No broker account linked", with the button "Add Groww account").
- No illustrations.
