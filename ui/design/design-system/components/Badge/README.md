# Badge

A small uppercase tag for metadata: broker name, "CSV import only", "Unmapped", "Later", conviction.

- **Props:** `tone` (`neutral`, `solid`, `accent`, `up`, `down`, `warn`, `review`), `children`, `title`.
- Keep badges to one or two words.
- A badge is never a verdict. Use `VerdictChip` for verdicts.
