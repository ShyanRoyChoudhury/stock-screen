# Icon

The system's own 16px line icons (1.5px stroke, square caps).

- **Props:** `name`, `className`, `title` (omit it for decorative use).
- They inherit `currentColor`.
- Names are listed on `Icon.names`.
